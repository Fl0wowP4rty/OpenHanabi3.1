package com.sun.scenario.effect.impl;

import com.sun.scenario.effect.Filterable;

public interface PoolFilterable extends Filterable {
   void setImagePool(ImagePool var1);

   ImagePool getImagePool();
}
